# Export Module - Audit Pass 1

Module: export (client CSV engine)
Scope: admin web + shared types
Source verified: 2026-09-08 UTC
Access rule: admin web = admin only

## 1. Selected Module

export page (paginated fetch + sanitize + CSV download + audit log-export).

## 2. Findings Fixed

| Severity | Finding                                                                                       | Fix                                                     | Files                                         | Status  |
| -------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------- | --------------------------------------------- | ------- |
| BROKEN   | One-level flatten collapsed tenant.user to name only                                          | Recursive dotted flatten (depth 3, name/label fallback) | export/page.tsx                               | WORKING |
| MISSING  | 7 list APIs unsupported (floors/rooms/guardians/notices/menus/laundry-slots/washing-machines) | Options + ExportResource union extended                 | export/page.tsx, packages/types/src/export.ts | WORKING |
| SHALLOW  | Generic export failure message                                                                | parseApiError appended                                  | export/page.tsx                               | WORKING |

## 3. Verified Working (no change)

| Area                                              | Proof                                         |
| ------------------------------------------------- | --------------------------------------------- |
| Page-walk to 20k rows with meta/short-page guards | handleExport loop                             |
| Sensitive-key stripping (password/token/secrets)  | SENSITIVE_PATTERNS (dotted paths still match) |
| Formula-injection guard                           | sanitizeCSVValue                              |
| log-export audit write                            | audit-logs/log-export call                    |

## 4. Verification

bun run lint clean; bun run typecheck clean; no tests executed; no emojis; portal boundaries respected.
