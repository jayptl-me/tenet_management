# Auth Module - Audit Pass 1

Module: auth (login/session/password/me)
Scope: admin web login + API + Flutter auth (read-only mapping)
Source verified: 2026-09-08 UTC
Access rule: admin login on web only; tenant/guardian on Flutter only

## 1. Selected Module

auth routes (login, refresh, me, password, reset) + login page + Flutter reject-admin.

## 2. Findings Fixed

| Severity | Finding                                                          | Fix                                                    | Files                       | Status  |
| -------- | ---------------------------------------------------------------- | ------------------------------------------------------ | --------------------------- | ------- |
| BROKEN   | toPublicJSON returned passwordHash (login fetched +passwordHash) | Strip passwordHash/loginAttempts/lockedUntil/ntfyTopic | apps/api/src/models/user.ts | WORKING |
| MISSING  | /auth/me enriched tenantId only; guardians got no ward link      | guardianId + wardTenantId enrichment (best-effort)     | apps/api/src/routes/auth.ts | WORKING |

## 3. Verified Working (no change)

| Area                                                                               | Proof                                                        |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| Web login rejects tenant/guardian; Flutter rejects admin                           | login page + auth_repository + AdminLayout/app_router guards |
| Lockout, inactive block, password>=8, token revoke, enumeration-safe reset         | auth.ts routes                                               |
| Tenant tenantId self-heal on /me                                                   | auth.ts P0-F1 block                                          |
| Server session minted on cross-portal login is inert (role guards deny everything) | middleware coverage; revoke-on-reject not needed             |

## 4. Verification

bun run lint clean; bun run typecheck clean across types/web/api; no tests executed; no emojis; portal boundaries respected.
